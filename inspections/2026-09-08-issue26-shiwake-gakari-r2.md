# 検品報告（r2）: Issue #26 仕分け係スキル（配布 zip）と実験 E2 ― ブランチ kazumasakawahara/zip-e2  2026-09-08

r1（inspections/2026-09-08-issue26-shiwake-gakari.md）の差し戻し3点と、r1 以降に変わったファイルの F1・F2 だけを当てた。他の項目は r1 の状態を引き継ぐ。

## 0. 検品者の限界（先に読むこと）
- 検品者: Claude Fable 5.1（この部屋のセッション）／作業者: Orca 上の Claude Code（モデル不明。3回目の仕分け実演は Fable 5.1 と記載）→ モデル系統は同一（非独立）。セッションは独立
- 精査できた範囲: r1 の先端 4d8a18f 以降の3コミット（9ファイル）の差分全量。zip は全エントリの属性を読み、実際に `unzip` で展開して stat を取った
- 使えなかった手段: 「3回目の返事は無加工」は検証手段なし（r1 と同じ）。Obsidian でプロパティがリンクとして数えられるかは未実測
- 見落としの可能性が高い領域: log-E2.md の3回目本文（読了のみ）

## 1. 受入基準の出所
- r1 第6節の差し戻し文（3点）と、Issue #26 完了条件（A7・B1）。「してはいけないこと」: raw／_テンプレート を触らない、push しない

## 2. 作業者の主張（未検証として隔離）
- zip を属性を直して作り直した。展開後はフォルダ drwxr-xr-x、ファイル -rw-r--r--
- 日々/ 3ノートと ページ/ 2枚を v1.0 SKILL の3回目出力に差し替えた（潮見町.md は同一で差分なし）
- log-E2 に3回目を無加工で追記。台帳のコミット数を訂正。計8コミット、push なし

## 3. 固定項目（r1 以降の変更分）
| # | 項目 | 手段 | 観測した事実 | 状態 |
|---|---|---|---|---|
| F1 | 範囲外変更 | `git diff --name-status 4d8a18f zip-e2` | 変更9ファイル。zip・日々/3・ページ/2・log-E2・台帳2。差し戻しの範囲内。raw／_テンプレート の履歴は追加の1件のみで変化なし | 確認 |
| F2 | 静かな喪失 | `--numstat`、zip の中身を r1 の zip とバイト比較 | zip 内の6ファイルは r1 と同一（属性だけ変更）。ノート・ページは差し替え目的の行のみ置換。log-E2 は追記27行・書き換え2行（「そのままコミット」の記述を差し替えた旨に） | 確認 |

## 4. 差し戻し項目
| # | 差し戻し | 手段 | 観測した事実 | 状態 |
|---|---|---|---|---|
| R1 | zip のディレクトリ属性 | zipfile で全エントリ属性、空フォルダに `unzip`、`stat` | `shiwake-gakari/`・`templates/` が 0o40755、ファイル 0o100644。日本語名3枚は UTF-8 フラグあり。展開後 drwxr-xr-x／-rw-r--r--、`ls templates` で3枚が見える | 確認 |
| R2 | 保管庫を v1.0 の出力に差し替え | 3ノート・3ページの全文、raw の履歴、ページ一覧 | `about: ["[[岬の灯台]]"]`・`["[[浜食堂]]"]`・`[]`、`place: ["[[潮見町]]"]`、`source` は raw のファイル名。本文は r1 と同じ（`[[ ]]` の囲みのみ）。ページは3枚、「おかみさん」なし。ページの一行から `[[潮見町]]` の囲みが外れた（作法の範囲内、log-E2 の2回目の記載と一致）。raw は r1 と同一 | 確認 |
| R3 | 台帳のコミット数 | 台帳の差分、`git log` | 「5コミット（最後は台帳分）」に訂正、検品対応3コミットを加え計8と記載。実際の本数と一致 | 確認 |
| A6' | 3回目の返事が無加工 | log-E2 読了 | 4行の型どおり。無加工かは手段なし | 確認（読了のみ） |
| A8 | push していない | `git ls-remote --heads origin` | zip-e2 は origin に無い | 確認 |

## 5. 人間が確認すべき箇所（優先順）
1. r1 第5節の3（「一字一句」と `[[ ]]` 付加の線引き）は基準側の判断で、成果物では決着しない。記事で「AIの返事・そのまま」と書く前に決める。
2. r1 第5節の4（返事が無加工かは手段なし）は r2 でも同じ。記事に載せる前に log-E2 の本文を読む。
3. Finder のダブルクリック展開は未実測。配布前に一度、Mac で zip をダブルクリックして `templates` フォルダが開くことを見る。

## 6. 差し戻し文
- なし（r1 の3点は解消）
