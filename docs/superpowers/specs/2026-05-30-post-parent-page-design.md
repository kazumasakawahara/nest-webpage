# 「親なき後」ページ 設計仕様

- **日付**: 2026-05-30
- **対象ファイル**: `src/pages/post-parent.astro`（新規）、`src/lib/site.ts`（navLinks に1項目追加）
- **URL**: `/post-parent/`
- **ブランチ**: `feature/members-area`
- **ステータス**: 設計合意済み（実装計画前）

## 1. 背景と目的

「親なき後」は、知的・発達・精神障害のある本人を、親や家族が亡くなった後も地域で安心して暮らし続けられるようにするための備えを指す。nest の核心的な想いは次の通り：

> **お金を遺すことが、必ずしも本人のためになるとは限らない。**
> むしろ、親・家族が長年かけて蓄積してきた「我が子を守るための暗黙知」と、支援者たちの経験知を遺し、本人支援が親なき後も機能し続ける「仕組み」をつくることが本質である。

このページは、その理念を公開し、nest の関連する取り組み（人によるネットワーク＝くらしサポートネット／仕組みによる継承＝開発中の支援データベース構想）を束ねて伝える **ハブページ** とする。

## 2. スコープと制約

- 今回のスコープは **公式サイトの公開1ページ**（理念＋構想の提示）で完結する。
- 開発中システム（`neo4j-agno-agent` /「親なき後支援データベース」、`nest-support` /「Claude-Native 親なき後支援データベース」）は **概念紹介のみ**。実システムの統合・デプロイ・認証は今回のスコープ外で、将来の会員エリア課題として分離する。
- **会員限定ページ「自分ノート」（`/members/my-note/`）には触れない。** ハブは「くらしサポートネット」と「構想」の2本柱に絞る。
- **プライバシー**：実在の利用者・家族の個人情報、実際の支援データは一切掲載しない。システムは概念・狙いの説明に留める。

## 3. ページ構成（5セクション）

既存コンポーネント（`BaseLayout` / `Hero` / `CtaBlock`）と既存グローバルクラス（`.section` / `.section--sand` / `.container` / `.container--narrow` / `.eyebrow` / `.section__title` / `.reveal`）を流用。新規CSSは最小限（GHページと同方針）。

### 3-1. ヒーロー（`Hero`, `size="md"`）

- `eyebrow="Post-Parent Support"`
- `title="親なき後"`
- `lead="遺すのは、お金ではない。"`
- `placeholderLabel="POST-PARENT SUPPORT"`（実写画像は未定。用意され次第 `image` prop に差し込む）

### 3-2. 理念ブロック（主役）— `.section` / `.container--narrow`

- eyebrow「Our Belief」＋見出し「お金より、遺すべきものがある」
- 本文（コピーは §4）：「親が亡くなった後、本人を一番に考えてくれるのは誰か」という問いを起点に、お金を遺すことが本人のためになるとは限らないという nest の想い、そして「暗黙知・経験知を遺し、本人支援の仕組みをつくる」という目的を明言する。

### 3-3. くらしサポートネット（人のネットワーク）— `.section--sand` / `.container--narrow`

- eyebrow「People Network」＋見出し「人と人とで、支え合う」
- 本文：本人・家族・支援者がつながり、互いの経験を持ち寄って支え合う取り組みとして「nestくらしサポートネット」を紹介する。
- **注記**：くらしサポートネットの具体的な活動内容について、河原さん／林代表からの事実確認が必要（§6参照）。本仕様の §4 コピーは事実に反しない範囲の汎用的な草案。

### 3-4. 構想：暗黙知をデジタルで継承する — `.section` / `.container--narrow`

- eyebrow「Our Challenge」＋見出し「知恵を、引き継げる形に」
- 本文：nest が開発を進めている「親なき後支援データベース」の **構想** を、機微情報に触れずに概念として紹介する。「親が積み重ねた“我が子を守る知恵”を、支援者みんなで引き継げる形にする」という狙い。**「現在開発中」** であることを明示し、実在の支援データは扱わない旨を一言添える。
- 末尾に控えめな一文で問い合わせへの橋渡し。

### 3-5. 問い合わせ動線（`CtaBlock`, `variant="dark"`）

- `title="親なき後の備えについて"`
- `body="ご家族の備えや、わたしたちの取り組みにご関心のある方は、お気軽にお問い合わせください。"`
- `primary={{ label: 'お問い合わせフォーム', href: '/contact/' }}`
- `secondary={{ label: 'nestについて', href: '/about/' }}`

## 4. 本文コピー（草案・確認後に確定）

### 理念ブロック

> 「親がいなくなったら、この子のことを一番に考えてくれるのは誰だろう」——障害のある子をもつ親にとって、これは切実な問いです。
>
> 多くの場合、その備えとしてまず「お金」が考えられます。けれどもわたしたちは、お金を遺すことが必ずしも本人の幸せにつながるとは限らない、と考えています。本当に必要なのは、親や家族が長い年月をかけて積み重ねてきた「我が子を守るための知恵」——どんなときに不安になるのか、何を大切にしているのか、誰が支えてくれるのか——そうした暗黙知を、言葉にして遺すことです。
>
> nest がめざすのは、その知恵と、支援者たちの経験を持ち寄り、親なき後も本人の暮らしを支え続けられる「仕組み」をつくることです。

### くらしサポートネット（事実確認後に調整）

> 「nestくらしサポートネット」は、本人と家族、そして支援者がゆるやかにつながり、互いの経験を持ち寄って支え合うための取り組みです。ひとつの家族だけで抱え込むのではなく、地域のネットワークとして本人の暮らしを見守っていく——その土台づくりを続けています。

### 構想セクション

> わたしたちは、親や支援者が蓄積してきた知恵を、特定の誰かの記憶だけに頼らず、支援に関わる人たちみんなで引き継いでいけるようにする仕組みづくりに取り組んでいます。
>
> 本人の医療・生活の情報、大切にしていること、緊急時に気をつけること、支えてくれる人のつながり——こうした情報を整理して残し、必要なときに必要な人へ確実に引き継げる形にする。それが、親なき後も途切れない支援を実現するための、nest 発の挑戦です。
>
> ※ この仕組みは現在開発中です。実際の利用者・ご家族の情報を本ウェブサイト上で扱うことはありません。

## 5. 技術メモ

- 新規ファイル `src/pages/post-parent.astro`。`group-home.astro` と同じ骨格（`BaseLayout` + `Hero` + `section` + `CtaBlock`）。
- `src/lib/site.ts` の `navLinks` に、`{ label: 'グループホーム', href: '/group-home/' }` の直後へ `{ label: '親なき後', href: '/post-parent/' }` を追加する。
  - 注：`site.ts` には既存の未コミット WIP（tagline 等）がある。本変更は navLinks への1行追加に限定し、他の WIP には手を触れない。
- 新規CSSは可能な限り書かず、既存グローバルクラスとインライン `style`（GHページと同じ書式）で対応する。

## 6. 未確定事項（実装前に河原さんへ確認）

- **くらしサポートネットの活動実態**：§4 のコピーは汎用的な草案。実際の活動内容（対象・頻度・具体的な支え合いの形など）が分かれば、それに合わせて文言を調整する。事実が確認できない場合は、現状の汎用コピーのまま掲載する。

## 7. 検証

- `npm run build` がエラーなく通り、`/post-parent/` ルートが生成される。
- ナビ「親なき後」リンクが `/post-parent/` に到達する。
- ページ内に実在の利用者・家族の氏名・住所・実支援データが含まれていない（目視＋grep）。
- 開発中システムは「開発中」と明示され、実データ非掲載の旨が記載されている。

## 8. 後続

- 当事者研究ページ（情報待ち）。
- 将来：開発中システムの会員エリア統合（別プロジェクト）。
