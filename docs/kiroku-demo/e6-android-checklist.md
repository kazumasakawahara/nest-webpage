# E6／E5 実機確認メモ ― Android ＋ Mac ＋ Google ドライブ（番外編の公開ゲート）

番外編 `src/content/ai-tips/2026-09-10-kiroku-ex-sumaho.md` は `draft: true`。
このメモの **一段目（必須）** が通ったら draft を外して公開する。二段目は通った範囲だけ記事に残す。

記事の「この記事の約束」に「公開前に筆者の実機で一度通した範囲だけを書きました」とあるので、
通らなかった項目は記事から削るか書き換える（約束を嘘にしない）。

## 準備（Mac 側）

- [x] パソコン版 Google ドライブを入れ、設定で **ミラーリング** を選ぶ（ストリーミングではない）— 2026-09-10 済。マイドライブは `/Users/k-kawahara/GoogleDrive`
- [x] 試験用の保管庫 `記録-test/`（中に `raw/`）をドライブのフォルダの中に作る。本番の保管庫は触らない — Finder で作成（2026-09-10）
- [x] Claude Desktop の Filesystem に `記録-test/` を見せる（2026-09-10 済）。実パスは `/Users/k-kawahara/GoogleDrive/記録-test`（CloudStorage 側の「マイドライブ」はここへのシンボリックリンク）
- [x] Obsidian（Mac）で `記録-test/` を「既存の保管庫を開く」で開ける — 2026-09-10 済

## 一段目（必須）― 写真だけ

| # | 確認すること | 結果 | メモ |
|---|---|---|---|
| 1-1 | Android のドライブアプリで、写真の「共有」→ ドライブ → 保存先に `記録-test/raw` を選べる | ✅ 2026-09-10 | **道が違った**。ドライブアプリ内の写真の「共有」は人への共有（「ユーザーまたはグループを追加」）。通ったのは **ドライブアプリで raw を開く → 右下「＋」→「アップロード」→ 写真を選ぶ**。記事の一段目 手順4 を書き直す（写真アプリからの「共有→ドライブに保存」は未確認） |
| 1-2 | 数分以内に Mac の `記録-test/raw/` に写真が現れる | ✅ 2026-09-10 | スマホが Wi-Fi 未接続の間「バックアップ中」表示のまま止まっていた。接続後に届いた（2回送ったため「(1)」付きの複製も届いた）。時間差の罠に「通信がないと止まる」を足す |
| 1-3 | 現れた写真に雲マークがつかず、Finder の「情報を見る」でサイズが実体 | ✅ 2026-09-10 | 2枚とも約3.7MB、JPEG ヘッダあり、ディスクブロックあり（部屋で機械確認）。マイドライブは `~/GoogleDrive` にミラーリング。共有ドライブはストリーミングのまま（箱には使わない） |
| 1-4 | Obsidian（Mac）の raw に写真が見える | ✅ 2026-09-10 | 保管庫 記録-test を開き、raw に JPG 2枚 |
| 1-5 | Claude Desktop で「raw の新しいものを仕分けて」（仕分け係 v1.1 を置いた状態）→ ノートができる | ✅ 2026-09-10（軽い版で確認） | 仕分け係は置かず、「raw の一覧と写真の中身を教えて」で確認。Filesystem が 2 ファイルとサイズ（3.58MB）を返した。**写真の中身は Filesystem の読み込み上限（1MB）を超えて読めなかった**（縮小版を作る提案が返った）→ 記事の罠5「写真の中身は読めないことがある。仕分けはコメントで決まる」を追加 |
| 1-6 | Mac で raw に一行ノートを書き、写真を貼ると、写真が raw に入る（第1回の形が Mac 側で成立） | 省略 | 第1回と同じ形のため、河原さん判断「これで十分」で省略（2026-09-10） |

## 二段目（通った範囲だけ記事に残す）― コメントもスマホで

2026-09-10 時点で **未実施**。記事は二段目の手順を残したまま、「約束」と二段目の冒頭に「筆者の実機では未確認。公式ヘルプと各アプリの説明の範囲」と明記して公開する（河原さん判断）。

| # | 確認すること | 結果 | メモ |
|---|---|---|---|
| 2-1 | 同期アプリ（FolderSync または Autosync for Google Drive）で、ドライブの `記録-test` とスマホ内の `記録-test` を **双方向** に組にできる | | どのアプリを使ったか、無料枠で足りたか |
| 2-2 | Obsidian モバイルで、スマホ内の `記録-test` を保管庫として開ける | | 「既存の保管庫を開く」の場所の選び方 |
| 2-3 | 設定「ファイルとリンク → 新規添付ファイルの保存先」を `raw` にできる | | |
| 2-4 | raw に新しいノートを作り一行打つ → 数分以内に Mac の raw に `.md` が現れ、中身が読める | | かかった時間 |
| 2-5 | ノートに写真を貼る → 写真が raw に入り、Mac にも実体つきで届く | | |
| 2-6 | **E5**: キーボードのマイク（音声入力）で Obsidian のノートに話すと、文字になる | | 記事第2回の callout の裏取り |
| 2-7 | Mac で仕分けたあと、`日々/` `ページ/` がスマホの Obsidian に戻ってきて、グラフ表示に糸が出る（場面三） | | |
| 2-8 | 同時編集の衝突：Mac とスマホで同じノートをほぼ同時に直したとき、何が起きるか（競合コピーの名前） | | 起きたら記事の罠に一行足す |

## 罠の裏取り

| # | 確認すること | 結果 |
|---|---|---|
| 3-1 | パソコン版ドライブを一時的にストリーミングにすると、写真に雲マークがつき Filesystem が読めない（記事の罠1の裏取り。確認後ミラーリングに戻す） | |
| 3-2 | 放り込んでから Mac に届くまでの体感時間（記事は「数十秒から数分」と書いている） | |

## 結果の書き戻し先

- 通った／通らなかった項目 → この表。記事の該当手順を直す
- アプリ名・画面の言い回しの違い → 記事の手順の文言
- 二段目が一つも通らなければ、二段目を「公式ヘルプによれば」の紹介にとどめ、手順の番号リストを外す
